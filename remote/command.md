#### BRANCH

```bash

# Displays remote branches
    #  origin/HEAD -> origin/main
    #  origin/main
git branch -r

# Displays all branches
git branch -a
    #   feature
    # * main
    #   remotes/origin/HEAD -> origin/main
    #   remotes/origin/main

```
